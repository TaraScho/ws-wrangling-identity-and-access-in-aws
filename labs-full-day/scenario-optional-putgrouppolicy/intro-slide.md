# Optional Scenario — PutGroupPolicy

Time-permitting; outline marks this as "if time" in the afternoon.

## Slide intro bullets

- `iamws-group-admin` user has `iam:PutGroupPolicy *` and belongs to the `iamws-group-admins` group.
- Write an admin inline policy to your own group → instant admin.
- This was originally Exercise 2 in the 2-hour version — small enough to also serve as warmup material if scenario 1 runs short.
- Pathfinding.cloud category: IAM-PutGroupPolicy.

## Learning objective

Participants recognize self-targeting via group membership as a variant of "write to a resource attached to yourself" — same family as CreatePolicyVersion (Scenario 1).
